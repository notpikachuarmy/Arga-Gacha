# Arga Gacha

Prototipo web creado con Phaser, TypeScript y Vite.

## Desarrollo local

```bash
npm ci
npm run dev
```

## GitHub Pages

El workflow `.github/workflows/deploy.yml` compila y publica automáticamente el juego al hacer cambios en `main`.
